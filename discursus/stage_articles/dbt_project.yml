name: 'discursus_stage_articles'
version: '0.1.0'
config-version: 2

profile: 'discursus_stage_articles'

model-paths: ["models"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  discursus_stage_articles:
    +materialized: table